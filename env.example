# Пример переменных окружения (документация)
#
# Docker Compose автоматически читает файл `.env` рядом с `docker-compose.yml`.
# Для локальной разработки эти переменные НЕ обязательны (если используете профили).

# Backend
JWT_SECRET=CHANGE_ME_IN_PROD
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Telegram (обязательно, если включаете профиль telegram или prod compose)
# TELEGRAM_BOT_TOKEN=your_token_here

# Web (если поднимаете web отдельно)
# VITE_API_BASE=http://localhost:8000

# Если `npm install` в Docker "висит", попробуйте задать registry (или зеркало):
# NPM_REGISTRY=https://registry.npmjs.org/
