<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ExecAl" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Compose использует MaterialTheme внутри, но базовая тема нужна для Activity -->
        <item name="android:statusBarColor" tools:targetApi="l">?android:colorBackground</item>
    </style>
</resources>

